export enum Gender {
    MALE = 'male',
    FEMALE = 'female',
}
export enum CivilStatus {
    SINGLE = 'single',
    MARRIED = 'married',
    DIVORCED = 'divorced',
    WIDOWED = 'widowed',
    SEPARATED = 'separated',
}
export enum LanguageCode {
    EN = 'en',
    ES = 'es',
}
